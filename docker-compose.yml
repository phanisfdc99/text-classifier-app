services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"

  ui:
    build: ./ui
    ports:
      - "8501:8501"
    environment:
      - API_URL=http://backend:8000
    depends_on:
      - backend
